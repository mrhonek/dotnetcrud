{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  },
  "crons": [
    {
      "schedule": "0 2 * * *", 
      "command": "dotnet ASPNETCRUD.API.dll --reset-database",
      "service": "aspnetcrud-api"
    }
  ]
} 